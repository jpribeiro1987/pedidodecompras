version: '3.8'

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - prisma_data:/app/prisma
    environment:
      - DATABASE_URL=file:./dev.db
      - NODE_ENV=production

volumes:
  prisma_data:
